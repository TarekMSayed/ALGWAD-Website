services:
  hugo:
    build: .
    ports:
      - "1313:1313"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    working_dir: /app
    command: hugo server -D --bind 0.0.0.0 --baseURL http://localhost:1313/ --poll 700ms

volumes:
  node_modules:
